[tool.poetry]
name = "mighty-agent"
version = "0.1.0"
description = "Mighty Agent"
authors = [
// ...existing code...
]
readme = "README.md"
packages = [{include = "mighty_agent"}]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.103.1"
// ...existing code...
uvicorn = {extras = ["standard"], version = "^0.23.2"}


[tool.poetry.group.dev.dependencies]
// ...existing code...
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.uvicorn]
host = "0.0.0.0"
port = 8000
reload = true
factory = "mighty_agent.web.application:get_app"